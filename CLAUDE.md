# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

---

# SAKURA JAPAN BEAUTY — プロジェクト恒久ルール

このファイルは、Claude Code が**毎回必ず参照する恒久ルール**です。
個別の指示とこのファイルが矛盾する場合は、まず矛盾を指摘し、確認を取ってから進めること。
このファイルに書かれていない仕様を勝手に決めない。判断に迷ったら「未決定事項」（末尾）へ追記して確認する。

**現在のフェーズ：Phase 2 進行中（海外向け有料販売の接続層）。**
Phase 1（UI/UX・多言語構造・管理画面骨格）は完了。
Phase 2 では、決済・データベース・メールの**接続口**まで実装済み。
実際の鍵が入るまで、それぞれの機能は自動的に無効になる（`src/lib/env.ts`）。

コードは `sakura/` にあります。開発手順は `sakura/README.md`、Phase 1 の計画は `docs/phase-1-plan.md` を参照。

---

## 1. プロダクト概要

**ブランド仮称：SAKURA JAPAN BEAUTY**

日本人美容講師「SAKURA」をブランドの顔として、日本の美容を世界へ販売する**オンライン教育プラットフォーム**。

**単なる動画販売サイトではない。**「日本の美容サロンそのもの」を教育商品として販売する。

扱う教育領域：

- 美容技術
- 接客
- おもてなし
- 細かな気配り
- カウンセリング
- 衛生管理
- サロン空間
- サロン経営
- スタッフ教育
- リピート
- 店販
- 日本の美容が海外から評価される理由

**ターゲット：** 海外の美容従事者・サロンオーナー・開業希望者。

**このサービスは日本人向けではない。主対象は海外（英語・繁体字中国語・韓国語圏）。**
日本語版も用意するが、日本国内で売ることを主目的にしない。
UX・価格・決済・メール・講座画面・販売ページの判断は、すべて海外ユーザーを優先する。
対象は「日本の美容・サロンサービスを、自分の国で取り入れたい人」。

**将来展望：** 今回作る教育販売システム自体を他社へ提供できる可能性を考慮する。
ただし**初期段階は SAKURA JAPAN BEAUTY での利用を最優先**し、必要以上に複雑な SaaS を作らない。
（= マルチテナント化を前提とした過剰設計をしない。ただし後から分離できないほど密結合にもしない）

---

## 2. 登場人物

| 人物 | 立場 | 担当領域 |
|---|---|---|
| **SAKURA** | OWNER・ブランドの顔・**サイト全体の主人公** | 日本式サロンスタンダード／日本式サロン経営／日本のおもてなし／日本の美容技術／美容・メイク／日本美容・インバウンド知識 |
| **TOMOMI** | INSTRUCTOR ADMIN（別講師） | フェムケア基礎／女性のライフステージ／美容従事者向けフェムケア／更年期前後の基礎教育 |

SAKURA をサイト全体の主人公として扱う。TOMOMI は独立した講師として並置するが、ブランドの中心ではない。

---

## 3. 絶対ルール（違反は差し戻し対象）

### 3-1. 管理画面はすべて日本語

- SAKURA・TOMOMI が触る**すべての裏側 UI は日本語**にする。
  対象：管理画面／設定画面／分析画面／講座作成画面／AI 作業画面／**エラー画面**／マニュアル／通知／確認ダイアログ／バリデーションメッセージ／空データ時の文言。
- 管理画面上に**不要な英語を表示しない**（ボタン、ラベル、メニュー、ステータス、グラフの凡例、日付表記まで含む）。
- 内部コード・変数名・API 名・DB カラム名が英語なのは問題ない。
- 海外向けの英語・韓国語・繁体字中国語コンテンツを管理画面で扱う場合は、**必ず日本語原文または日本語確認訳を併記**する。
- **SAKURA は英語を理解しなくてもすべて運営できることを絶対条件とする。**
  エラーコードだけを出して終わりにしない。「何が起きたか・どうすればいいか」を日本語で書く。

### 3-2. 実装の進め方

- **コードを変更する前に必ず既存プロジェクトを確認する。**
- **見ていないコードについて推測で回答・変更しない。**
- 既存機能を不用意に壊さない。
- **実装は Phase 単位。すべてを一度に実装しない。各 Phase 完了後に確認を取ってから次へ進む。**
- **本番環境を直接変更しない。** 確認環境 / staging を経由して本番へ反映する構成にする。
- 重要な変更は履歴を残す。

### 3-3. 完成の定義

「機能を実装しただけの素の Web ページ」「ページが表示された」を**完成扱いにしない**。
第 9 章「完了の定義」のチェックを通ったものだけを完成とする。

---

## 4. 対応言語 / 多言語設計

購入者向け（表側）：

| locale | 言語 |
|---|---|
| `ja` | 日本語（**MASTER＝原本**） |
| `en` | 英語 |
| `ko` | 韓国語 |
| `zh-TW` | 繁体字中国語 |

- **日本語を原本（MASTER）とする。翻訳版は日本語原本から派生する。**
- 日本語原本が更新されたら、古い翻訳版について**「翻訳更新が必要」と管理画面で判別できる設計**にする。
  （原本の更新時刻／バージョンを持ち、翻訳側と比較してステータス表示する等）
- 翻訳ステータスは管理画面上で日本語表示（例：「翻訳済み」「翻訳更新が必要」「未翻訳」）。
- 管理画面自体は日本語のみ（多言語化しない）。

### 日本語フォールバックの禁止（重要）

- **顧客向け画面で、未翻訳のときに日本語を表示しない。** 各言語の「準備中」を出す。
- 顧客向けの取り出しは `tc()` / `tcText()` / `tcList()`（`src/lib/format.ts`）。
  **`t()` は日本語へ落ちるため管理画面専用。**顧客向け画面で使わない。
- 既定ロケールは **`en`**（`src/i18n/routing.ts`）。言語が特定できない流入は英語へ寄せる。
  日本語は `/ja` で引き続き利用でき、**コンテンツの原本(MASTER)は日本語のまま**。
- 法務ページは日本語・英語のみ翻訳済み。韓国語・繁体字では日本語を出さず、
  「この言語では未提供・英語版が適用される」と明示して英語版へ誘導する。

---

## 5. ブランド・デザイン

### コンセプト

「日本」「桜の花びら」「美容」「清潔感」「上品」「シンプル」「高級感」「余白」

### 桜モチーフの使用範囲

**桜だらけにしない。** 使ってよい場所：

- ロゴ周辺
- 背景のごく薄い装飾
- セクション区切り
- 証明書
- 小さなワンポイント

**禁止：** 富士山・鳥居・着物・派手な和柄など、安直な日本表現の大量使用。
**禁止：** サイトをピンク一色にすること。

SAKURA 本人をブランドの顔として大きく見せる（写真を主役に使う）。

### 基本色（目安比率：白70% / 紺15% / 緑5% / 赤5% / 桜3%以下 / 金2%以下）

| 用途 | HEX |
|---|---|
| 白（全体背景。最も広く使う） | `#FFFFFF` |
| 藍・濃紺（**面として使う主役色**） | `#1F2D3D` |
| 松葉色・深緑（カテゴリー・補助ラベル） | `#3F5B4B` |
| 朱赤（主要CTA・重要な数字・短い罫） | `#A9362B` |
| 淡桜（**差し色のみ。面では使用禁止**） | `#E7CDD5` |
| 金（認定・罫線など少量のみ） | `#C2A15F` |
| 墨（文字） | `#252525` |
| 線 | `#D9D9D5` |
| 和紙（HERO・証明書など限定箇所のみ） | `#F7F5F1` |

- **基本背景は白。** 淡いベージュ・生成りの大きな面を繰り返さない。和紙テクスチャも多用しない。
- **濃紺を「面」として使う**セクションを設け、白 → 白 → 濃紺 → 白 のリズムをつくる。
  現在は「なぜ日本の美容なのか」「SAKURA のメッセージ」「フッター」が濃紺。
- **桜はピンク色ではなく「形」で感じさせる**（花びら1枚・線画の桜紋・区切りの小さな印）。
- 1画面で主役になる色は原則1色。多色でごちゃごちゃさせない。
- 補助文字色 `--color-ink-muted: #6E6E68` を追加（キャプション用）。

### 目指す印象

「可愛い」ではなく **品・静けさ・強さ**。**男性の美容師・サロンオーナーが見ても
「自分向けの専門教育サイトだ」と思えること**を必須条件とする。
淡いピンクの多用・柔らかすぎる色・丸い要素の多用・甘いデザインは避ける。
日本らしさは色だけでなく、**余白・細い線・静かなレイアウト・明朝の字間**で表現する。
角丸は 2px / 4px の2段階のみ。影はほぼ使わず、線と余白で構造を見せる。

---

## 6. UI 品質基準（Claude が特に注意する弱点）

### 禁止

- 機能を実装しただけの素の Web ページを完成扱いにすること
- **文字・見出し・ボタンだけを縦に並べたページ**
- 同じ種類の情報を**10 個以上縦に並べる** UI
- 装飾のためだけに要素を増やすこと
- 典型的な AI 生成 UI：巨大な見出し／大量の余白／意味のないグラデーション／巨大な丸角カードの乱用

### 情報の意味に応じて適切に使うもの

写真／統一された線画アイコン／カード／図／進捗表示／数字／ステータス／桜モチーフ／タブ／アコーディオン／ドロワー／横スクロール／固定ナビゲーション

### 目的

- **一目でどこに何があるかわかること。**
- ユーザーが大量にスクロールしなくていい設計を優先する。
- 項目が増える箇所は、タブ／横スライド／アコーディオン／「すべて見る」で整理する。

---

## 7. UX 絶対条件

- **1 画面 1 目的**を基本とする。
- よく使う操作は**管理画面 TOP から原則 2 クリック以内**。
- 購入者がログイン後「**続きから学ぶ」は 1 クリック**。
- 購入者が **PDF 資料へ到達するまで原則 2 クリック以内**。
- TOP ページから講座詳細・購入導線まで迷わせない。
- **スマートフォンを最優先で設計する。iPhone Safari を主要確認対象とする。**
- 主要 CTA をページ内で見失わせない。
- 長い商品詳細はタブやアコーディオンで整理する。
- スマートフォンの商品詳細では**購入 CTA を画面下部へ固定**できる構造を検討する。

---

## 8. アイコン

- **絵文字を UI アイコンとして乱用しない。**
- 無料で使用可能な**統一された線画アイコンライブラリ**を使用する（1 種類に統一。混在させない）。
- **アイコン＋日本語ラベル**で意味がすぐ分かるようにする。アイコン単独で意味を持たせない。

対応の目安：

| 項目 | アイコン |
|---|---|
| ダッシュボード | 家 |
| 売上 | グラフ |
| 講座 | 再生 |
| 受講者 | 人 |
| 投稿 | メガホン |
| クーポン | チケット |
| 認定 | メダル |
| AI | キラキラ |
| 設定 | 歯車 |
| 異常 | 警告 |

---

## 9. ユーザー権限

| ロール | 対象 | 権限 |
|---|---|---|
| **OWNER** | SAKURA | すべての情報・設定・売上・講座・講師・システムを操作可能 |
| **INSTRUCTOR ADMIN** | TOMOMI | 原則として**自分の**講座・売上・受講者・投稿・クーポン・お問い合わせを操作可能。SAKURA の重要設定／全体決済設定／システム設定／他講師情報は**変更不可** |
| **CUSTOMER** | 購入者 | 購入したコンテンツと自分の情報のみアクセス可能 |

- **将来的な細かな権限追加を阻害しない構造にする**（ロールを 3 つ固定でハードコードせず、権限判定を一箇所に集約する）。
- 権限チェックはサーバー側で行う。UI で隠すだけにしない。

---

## 10. 購入者向け基本機能

購入者ごとにマイページを持つ。

- 受講中講座
- 購入済み講座
- 進捗
- **続きから学ぶ**（1 クリック）
- PDF 資料
- 文字起こし
- 確認テスト
- 修了証
- 認定証
- クーポン
- おすすめ講座
- 購入履歴
- 問い合わせ
- アカウント設定

ルール：

- **PDF は購入者が何度でもダウンロード可能。**
- **動画は原則ストリーミング。直接ダウンロードさせない。**
- 基本的な**受講期限は無期限**を想定する。

---

## 11. 証明書

| 種別 | 対象 |
|---|---|
| **修了証** | 通常講座 |
| **認定証** | 認定対象講座 |

- **修了証と認定証を明確に分ける**（名称・デザイン・発行条件・表示すべてで区別する）。
- Web 上に**証明ページ**を持ち、以下を確認可能にする：
  - 氏名
  - 講座
  - 講師
  - 発行日
  - **Certificate ID**
- PDF 証明書には**Web 証明ページへ飛ぶ QR コード**を掲載できる構造を想定する。

---

## 12. 講座フォーマット

- 動画は基本 **16:9**。
- 講義動画の構成：
  - **左**：SAKURA または TOMOMI 本人がジェスチャーしながら説明
  - **右**：正方形の視覚教材
  - **下**：字幕
- **1 Lesson は原則 6〜12 分程度。**
### 動画の配信方式（暫定・コスト優先）

- **YouTube の限定公開動画を、購入者専用の講座ページに埋め込む。**
  初期費用を 0 円に抑えるため、Mux 等の有料配信サービスは現時点では導入しない。
- 動画は SAKURA がスマートフォンで撮影し、CapCut 有料版で編集する。
- **動画IDは、サーバー側で受講権限を確認したあとにだけ画面へ渡す。**
  権限が無いユーザーのHTMLには動画IDを一切含めない（`src/app/(site)/[locale]/learn/`）。
- 配信元は `src/lib/video.ts` に閉じ込める。将来 Mux 等へ移す場合はここと
  `VideoRef.provider` だけを直せばよい。
- YouTube 側では「限定公開」かつ「埋め込みを許可」に設定する。
  **URL流出を完全には防げないことは了解のうえで、コストと公開速度を優先している。**
- 中国本土からは YouTube を再生できない。繁体字の主市場は台湾・香港のため許容する。

- 動画内の正方形教材とは**別に**、以下を販売価値として提供可能にする：
  - 講義 PDF
  - Workbook
  - チェックリスト
  - 文字起こし
  - 確認テスト

---

## 13. 講座カテゴリー

**SAKURA**

- 日本式サロンスタンダード
- 日本式サロン経営
- 日本のおもてなし
- 日本の美容技術
- 美容・メイク
- 日本美容・インバウンド知識

**TOMOMI**

- フェムケア基礎
- 女性のライフステージ
- 美容従事者向けフェムケア

**重要：講師名だけで分類せず、「学びたい内容」から探せる導線を必ず用意する。**

---

## 14. 管理画面

### SAKURA 管理画面（経営・運営の司令塔）

メニュー：

- ダッシュボード
- 売上・分析
- 講座
- 講座制作
- 受講者
- 修了証・認定証
- 投稿・発信
- 商品・クーポン
- お問い合わせ
- AI 作業センター
- システム状況
- 設定

**ダッシュボード最上部**に重要数値を表示：

- 本日の売上
- 今月の売上
- 新規受講者
- 未対応件数

その下に「**今日やること**」と「**クイック操作**」を配置する。

### TOMOMI 管理画面

**SAKURA 画面より簡単にする。** 日常的に使う項目を中心に構成：

- 今日やること
- 自分の売上
- 講座
- 投稿
- お客様

---

## 15. 海外通貨・価格

- 顧客側では各通貨を表示可能にする。
- **管理画面では日本円換算を主に確認できる設計**にする（SAKURA が理解しやすいこと）。
- **外貨価格を日本価格の単純な為替換算で固定しない。市場ごとに独立価格を設定可能にする。**

### 言語（locale）と市場（market）・通貨は別管理

- `Locale` は表示言語、`MarketId` は販売市場。**「英語＝アメリカ＝USD」のような固定をしない。**
- 市場の定義は `src/lib/market.ts`（`global-usd` / `tw` / `kr` / `jp`）。
  顧客はフッターの切り替えで市場を選べる。選択は Cookie `sjb_market` に保存する。
  言語から決まるのは**初期値だけ**。
- 価格は `Course.pricing`（`src/lib/data/types.ts` の `Pricing`）。
  - `byMarket` に市場ごとの固定値。**為替換算のコードを書かない。**
  - `list` = 通常価格、`launch` = ローンチ価格（任意）。
  - `status: 'draft'` の間は**金額を表示せず、購入もできない**。
  - **その市場に価格が無ければ「準備中」。日本円へ落とさない。**
- 購入可否の判定は `isPurchasable(course, locale, market)` の1か所に集約する。

---

## 16. 売上・分析

最低限、将来的に以下を確認可能にする：

**金額系：** 総売上／純売上／返金／値引き／決済手数料
**分類軸：** 商品別／講師別／国別／言語別
**ファネル：** 商品閲覧／購入開始／購入完了／購入率
**学習：** 受講開始／修了率／Lesson 進捗
**販促：** クーポン利用

**数字を羅列するだけにしない。グラフやカードを適切に使う**（第 6 章の UI 品質基準を満たすこと）。

---

## 17. AI 運用

- **初期は AI API の追加課金をできるだけ発生させない。**
- 既に利用している **ChatGPT** と **Claude Code** を活用する。
- 管理画面には将来的に「**AI 作業センター**」を用意する。

役割分担（基本思想）：

| | 担当 |
|---|---|
| **考える・内容を作る** | **ChatGPT** — 講座企画／セリフ／教材／PDF／翻訳／商品説明／投稿／売上分析 |
| **決定済み仕様をコードにする** | **Claude Code** — 機能実装／バグ修正／DB／画面追加／API／システム変更 |

---

## 18. コスト方針・技術候補

### 追加課金

- **初期投資をできるだけ増やさない。**
- 既に利用中：ChatGPT / Claude Code / CapCut / Canva
- **Canva の AI 機能を前提にしない。** 教材・画像・PDF 等は ChatGPT 側で制作できる場合はそちらを優先する。

### 確定した技術スタック（Phase 1 で実測・確定）

| ライブラリ | バージョン | 備考 |
|---|---|---|
| Next.js | 16.3.4（App Router） | ルート直下は `(site)` と `(admin)` の2つの root layout |
| React | 19.2.8 | |
| **TypeScript** | **6.0.3** | **`latest` の 7.0.2 は使わない**（下記） |
| Tailwind CSS | 4.3.3 | CSS ファースト。`tailwind.config.js` は使わない |
| next-intl | 4.14.2 | |
| lucide-react | 1.41.0 | 線画アイコン。**これ1種類に統一**し、他のアイコンライブラリを混ぜない |
| **ESLint** | **9.39.5** | **10 系は使わない**（下記） |

**バージョンを上げてはいけない箇所（実際に壊れることを確認済み）**

- **TypeScript 7 系にしない。** `typescript-eslint@8.69.0` の peer が `>=4.8.4 <6.1.0` で、
  TS 7 では lint が動かない。CLAUDE.md 第19章が lint 通過を完了条件にしているため採用不可。
- **ESLint 10 系にしない。** `eslint-config-next` 同梱の `eslint-plugin-react` が
  ESLint 10 で削除された `context.getFilename()` を呼び、lint が実行時エラーになる。

> 依存を更新するときは、**必ず `npm run typecheck && npm run lint && npm run build` の3つが通ることを確認してから**行う。

### その他の技術判断（Phase 1）

- **Web フォントを読み込まない。** `next/font/google` はこの実行環境でフォント本体を取得できず、
  ビルドが失敗する。またフォント配信は海外からの表示速度に直結する。
  日本語=Hiragino/游/Noto、韓国語=Apple SD Gothic/Malgun、繁体字=PingFang TC/微軟正黑体 を
  ロケールに応じてOSが選ぶフォントスタックで対応している（`src/app/globals.css`）。
- **`middleware.ts` ではなく `src/proxy.ts`。** Next.js 16 で middleware 規約は非推奨になった。
- **Cloudflare へのデプロイは `@opennextjs/cloudflare`（Cloudflare Workers）を使う。**
  旧方式の `@cloudflare/next-on-pages`（Cloudflare Pages 用）は peer が `next <=15.5.2` で
  **Next.js 16 に非対応**のため使わない。設定は `sakura/wrangler.jsonc` と `sakura/open-next.config.ts`。
  Phase 1 は確認用のため R2 を使わない構成（再検証なし・事前生成データのみ配信）。
  ISR / 再検証が必要になった時点で R2 版へ差し替える。
- **`next.config.ts` で `agentRules: false`。** Next.js が `sakura/CLAUDE.md` を自動生成するのを止めている。
  このリポジトリの恒久ルールはルートの CLAUDE.md ただ1つとする。

---

## 19. 完了の定義（テスト思想）

**「ページが表示された」だけを完成としない。** 最低限、以下の視点で確認する。

**体験**

- 初めての人が**5 秒程度で何のサイトか理解できる**
- 目的ページまでのクリック数
- 不要なスクロール量

**環境**

- スマートフォン操作
- **iPhone Safari**
- レスポンシブ

**権限**

- SAKURA 権限
- TOMOMI 権限
- 購入者権限

**内容**

- 多言語
- リンク

**技術**

- TypeScript
- lint
- build

**状態**

- エラー状態
- 空データ状態
- ローディング状態

### End-to-End（後の Phase）

購入フロー全体を通しで確認する：

登録 → 購入 → 決済 → マイページ → 受講 → PDF → テスト → 修了 → 証明書

---

## 20. 現在の状況

- **Phase 1 完了。** 表側（TOP／講座一覧／講座詳細／講師紹介×2／無料コンテンツ／ログイン／新規登録／
  マイページ骨格）と管理画面（SAKURA用12項目・TOMOMI用7項目・ダッシュボード・売上分析・講座・受講者・
  お問い合わせ・投稿発信）を実装済み。4言語（ja/en/ko/zh-TW）の切替が動作する。
- **投稿・発信（`/admin/posts`）は SAKURA・TOMOMI の両方が使う。** 一覧と新規作成フォームを実装済み。
  講師ロールでは自分が書いた投稿だけを表示する。**保存は Phase 2**（フォーム送信時に日本語でその旨を表示する）。
  TOMOMI は AI 作業センターを使わない前提のため、引き続き非表示にしている。
- **クーポン（`/admin/products`）は TOMOMI にも表示する**（第9章の権限表に合わせた。講師向けラベルは「クーポン」）。
  講師が扱えるのは自分の講座に紐づくクーポンのみ。全商品対象・サイト全体・他講師の講座は対象にできない。
- **権限の唯一の正は `sakura/src/lib/admin-permissions.ts` の `adminRoutes`。**
  route と allowedRoles を並べた定義で、サイドメニュー・URLガード・ページガード・自動検査がすべてこれを参照する。
  **定義に無い管理URLはオーナー専用として拒否する（既定拒否）。**
  メニュー（`admin-nav.ts`）はラベルとアイコンだけを持ち、誰に見せるかは判断しない。
- **URLアクセス制御は `src/proxy.ts` で行う。** 全 `/admin/*` がここを通るため、
  ページ側で `ownerOnly()` を書き忘れても素通りしない。ページ側のガードは二重化として残す。
- **本番では管理画面を動かさない。** 認証が未実装のため、`ADMIN_DEMO_MODE=true` を
  明示した環境（開発・確認用）でのみ管理画面が開く。本番（`NODE_ENV=production` かつフラグなし）では
  レイアウトが `AdminLocked` を返し、Cookie を書き換えても OWNER にはならない
  （`getAdminRole()` が最小権限を返し、`setAdminRole()` も受け付けない）。
  **`wrangler.jsonc` に `ADMIN_DEMO_MODE` を設定しないこと。**
  管理レイアウトは `export const dynamic = 'force-dynamic'`（フラグを実行時に読むため）。
- **一覧データの絞り込みはリポジトリ層で行う。** `listDailySales` / `listCountrySales` も `role` を必須引数にしてあり、
  画面側が渡し忘れるとコンパイルエラーになる。
- **購入者向け領域（`/[locale]/mypage` と `/[locale]/learn`）はログイン必須。**
  `src/lib/session.ts` の `getSession()` が唯一の判定。認証が未実装のため、
  **本番（`SITE_DEMO_MODE` なし）では常に null を返す＝誰も入れない。**
  Cookie を偽造しても入れない（`npm run prod-guard-check` で確認できる）。
  `src/proxy.ts` でも1段目の判定を行うが、本体は各レイアウト／ページ側。
  **`wrangler.jsonc` に `SITE_DEMO_MODE` を設定しないこと。**
- **受講権限は `src/lib/entitlement.ts` の `hasEntitlement()` で判定する。**
  受講画面は権限を確認したあとにだけ動画IDを描画する。
  **決済成功ページの表示で権限を付けない。必ず Stripe の Webhook で支払いを確認してから付ける。**
- **日本式サロンスタンダードは全3章構成**（Japanese Beauty Philosophy /
  Japanese Salon Rules & Customer Experience / Bring Japanese Beauty to Your Country）。
  動画・教材ともに未制作のため、全章 `in-production`・全教材 `planned`・全言語未公開。
  価格も `draft`。**存在しない講座内容を完成済みとして見せないこと。**
- **法務ページ（`/[locale]/legal/{terms,privacy,refund,tokusho}`）は実装用ドラフト。**
  本文は `src/lib/legal.ts`。「［要確定］」は SAKURA が決める項目、
  「［要専門家確認］」は法律の専門家の確認が要る項目。**Claude 側で勝手に確定しない。**
  確定するまで `robots: noindex` を外さない。
### 決済・データベース・メール（Phase 2 で実装済みの接続層）

- **外部サービスは npm パッケージを足さず fetch で接続している。**
  Cloudflare Workers 上で確実に動かすためと、使う機能が限られているため。
  Stripe・Supabase・Resend いずれも SDK は入れない。
- **秘密の鍵はコードに一切書かない。** 入口は `src/lib/env.ts` だけ。
  未設定なら `isStripeConfigured()` などが false を返し、その機能は無効になる。
  設定手順は `sakura/docs/env-setup.md`（SAKURA 向けに画面名まで書いてある）。
- **受講権限を付与してよいのは `src/app/api/stripe/webhook/route.ts` だけ。**
  決済成功画面（`/[locale]/checkout/success`）は購入記録を**読むだけ**。
  URL を直接開かれるため、ここで付与すると未払いの人に権限が渡る。
  `npm run commerce-check` がこの一点を自動で検査している。
- **Webhook の冪等性は3段で担保する。** ①`webhook_events`（イベントID・処理済みなら何もしない）
  ②`purchases.stripe_session_id` の一意制約 ③`entitlements` の主キー。
  購入完了メールは②が「初めて記録した」ときだけ送るため、再送で二重に届かない。
  **途中で失敗したイベントは `processed_at` が空のまま残り、Stripe の再送で処理し直される。**
- **金額はクライアントから受け取らない。** 決済開始時に渡すのは Stripe の Price ID だけ。
  記録する金額は Webhook が Stripe から受け取った値のみを使う。
- **DB は service_role キーでサーバーからのみ接続する。** 全テーブルで RLS を有効にし、
  ポリシーを1つも作っていない（公開キーが漏れても1行も読めない）。
  スキーマは `sakura/supabase/migrations/0001_init.sql`。
- **購入時の同意（即時提供／解約権の放棄）は実装済み、文言は未確定。**
  実装（チェックボックス・必須判定・版の記録）は `src/lib/consent.ts` と `PurchaseForm.tsx`。
  **文言は法律の専門家の確認を経て差し替える。差し替えたら `CONSENT_VERSION` を必ず上げる**
  （過去の購入者がどの版に同意したか追えなくなるため）。確定したら `CONSENT_TEXT_APPROVED` を true にする。
- **メールは購入者が選んだ言語で送る。** 未対応の言語は日本語ではなく英語に落とす
  （`resolveMailLocale`）。管理側へ届く問い合わせ通知だけは日本語。
- **認証は未実装。** 決済に進めるのはログイン済みの人だけだが、そのログイン自体が
  まだ本物ではない（`getSession()` は本番で常に null）。ここが最後の未実装のP0。
- **AI API 接続は未実装。**

### 公開（Stripe 審査用の確認URL）

- **公開先は Cloudflare Workers。Worker 名は `sakura-japan-beauty`。**
  既存の Vi5 予約サイト（Vercel）とは別サービス・別名にしてあり、互いに影響しない。
  以前は Worker 名が `vi5-salon` で紛らわしかったため変更した。
- **手順は `sakura/docs/deploy.md`。** ブラウザだけで完結する手順（Cloudflare の GitHub 連携）を主にしてある。
- **検索エンジンへの掲載は既定で無効。** `SITE_INDEXABLE=true` を設定した時だけ載る。
  販売開始前のサイトが検索結果に出ると、あとから印象を直しにくいため。
  直接URLを開く分には影響しないので、Stripe の審査には支障がない。
- **公開環境に `SITE_DEMO_MODE` / `ADMIN_DEMO_MODE` / `SITE_INDEXABLE` を設定しないこと。**
  前2つを設定すると、管理画面と購入者画面が誰でも開ける状態になる。
- データはすべてモック。`sakura/src/lib/data/` のリポジトリ層経由で取得しており、
  Phase 2 で Supabase 実装に差し替えても画面側の変更は不要な構造にしてある。
- **次の指示があるまで Phase 2 を開始しない。**

### Phase 1 で決めたこと

| 項目 | 決定 | 理由 |
|---|---|---|
| 配置場所 | 既存リポジトリ内の **`sakura/`** サブディレクトリ | 新規リポジトリを作成する GitHub 権限が無いため。既存サロンアプリ（ルートの `index.html` 等）とは完全に分離し、`vercel.json` にも触れていない |
| 補助文字色 | `--color-ink-muted: #6B6361` を追加 | 第5章の7色には補助文字色が無く、キャプションと本文の階層が作れないため。**要事後承認** |
| 管理画面のURL | `/admin`（`[locale]` の外） | 日本語固定を構造で保証するため |
| 権限の出し分け | 同一 `/admin` ツリー＋`src/lib/admin-nav.ts` の1か所で制御 | 画面を二重に持たず、将来の権限追加を阻害しないため |

### 自動検査（実装を変えたら必ず流す）

| コマンド | 何を守るか |
|---|---|
| `npm run typecheck` / `npm run lint` / `npm run build` | 第19章の技術チェック |
| `npm run ux-check` | クリック数・言語切替・管理画面の権限 |
| `npm run launch-check` | **海外公開の事故防止**：未ログイン・未購入での到達、日本語混入、未確定価格の表示、通貨の固定、法務・問い合わせへの到達、404の言語、hreflang、スマホの横スクロール |
| `npm run prod-guard-check` | 本番相当で購入者向け画面と管理画面が閉じていること |
| `npm run commerce-check` | **決済の安全**：鍵がコードに無いこと、権限付与が Webhook だけであること、署名検証が偽物を弾くこと、冪等性の作り。サーバーは自分で起動するので事前準備は不要 |
| `npm run publish-check` | **公開前**：4言語の表示、日本語混入、法務・問い合わせへの到達、購入者画面と管理画面が閉じていること、HTMLに秘密情報が無いこと、検索エンジンへの非掲載。**本番と同じビルド成果物に対して流す** |

`ux-check` / `launch-check` は `SITE_DEMO_MODE=true npm run dev` を起動してから実行する。
`prod-guard-check` は逆に、**フラグを設定せずに** `npm run build && npx next start -p 3100` してから実行する。
`publish-check` は Cloudflare の実行環境で確認する：
`npm run cf:build && npx wrangler dev --port 3300 --local`（環境変数を設定せずに起動）してから
`BASE_URL=http://localhost:3300 npm run publish-check`。

### 実装上の落とし穴（実際に踏んだもの）

- **`not-found.tsx` の中でサーバー側の動的API（`headers()` / `getTranslations()`）を使わない。**
  例外にならず、中身が空のまま描画される。存在しないURLは
  `[locale]/[...rest]/page.tsx` がサーバー側で言語別に描く。
- **HTML を grep して画面の内容を判定しない。** 4言語ぶんの翻訳が
  クライアント用データとしてHTMLに埋め込まれるため、画面に出ていない文言も引っかかる。
  必ず描画結果（`innerText` / URL）で判定する。
- **実行時の環境変数を読むページには `export const dynamic = 'force-dynamic'` を付ける。**
  付けないとビルド時の値が焼き込まれ、フラグが効かない。
- **`cookies()` を読むと、そのページは動的レンダリングになる。**
  全ページ共通のフッターでサーバー側の `cookies()` を読むと、
  価格を出さないページまで遅くなる。市場の切り替えはクライアント側で Cookie を読む。

### このリポジトリについての注意

このリポジトリ（`sacuraupdate/vi5-salon`）のルートには、**別プロダクトである既存のサロン予約 PWA**
（`index.html` / `app.html` / `shop.html` / `api/` / `vercel.json` / `manifest.json` / 画像類）が入っている。
これは稼働中の実サービスであり、**SAKURA JAPAN BEAUTY とは別物**。誤って変更・削除しないこと。
その構成・データ保存方式は `docs/vi5-salon-app.md` に記載。

> **要確認：** SAKURA JAPAN BEAUTY を**この既存リポジトリ内に**構築するのか、**新しいリポジトリ**を用意するのか未決定。
> 実装開始前に必ず確認すること。

---

## 21. 未決定事項（実装前に確認が必要）

以下は本ドキュメントに記載がなく、実装前に決定が必要な事項。**勝手に決めない。**

1. **ドメイン** — 独自ドメインの有無、言語別 URL 設計（`/en/` などのパス方式か、サブドメインか）
3. **決済の詳細** — Stripe の対応国、通貨、海外の消費税・VAT 取り扱い、返金運用フロー
4. **法務ページ** — 利用規約／プライバシーポリシー／特定商取引法に基づく表記／返金ポリシーの内容と作成担当
5. **メール送信** — 購入確認・パスワード再設定・修了通知などの送信手段
6. **認定証の発行基準** — 「認定対象講座」の条件、確認テストの合格基準、再受験可否
7. **アカウント運用** — 会員登録方式（メール／SNS ログイン）、パスワード再設定、退会・データ削除
8. **問い合わせの対応言語** — 海外購入者からの英語等の問い合わせをどう受けるか
9. **staging 環境** — 確認環境の具体的な構成
10. **バックアップ・障害対応** — データバックアップ方針、「システム状況」画面で何を監視するか
11. **アクセシビリティ基準** — 目標とする水準
12. **ロゴ・ブランド素材** — SAKURA / TOMOMI の写真とロゴ。
    `sakura/src/components/brand/PhotoFrame.tsx` に `src` を渡すだけで差し替わる構造になっている
13. **講座の実データ** — Phase 1 のサンプル講座12件は確認用の仮データ。実際の講座名・価格・時間
14. **各市場の確定価格** — 通常価格・ローンチ価格とも未確定。
    暫定案は日本円で通常 59,800 / ローンチ 39,800。USD / TWD / KRW は未定。
    決まったら `src/lib/data/mock/catalog.ts` の `pricing.byMarket` に入れ、`status` を `'confirmed'` にする
15. **難易度の区分** — 現在は初級／中級／上級の3段階で仮置き

---

## 22. このファイルの更新ルール

- 仕様が決まったら**このファイルを更新する**。決定事項を会話の中だけに残さない。
- 「未決定事項」で決着したものは該当章へ移し、リストから削除する。
- ルールを変更する場合は、変更理由を残す。
