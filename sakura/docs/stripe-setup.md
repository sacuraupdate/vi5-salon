# Stripe 決済をはじめるために足りないこと（SAKURA 用）

**コード側の実装は終わっています。** 足りないのは、下の設定と登録だけです。

> **初回販売の対象は「米ドル・台湾ドル・韓国ウォン」の3市場です。**
> 日本円は P1（あとから追加）とします。
> そのため、いま作る Stripe の価格は **6つ**（3市場 × 通常価格／ローンチ価格）です。

進み具合は、管理画面の **「システム状況」** で確認できます。すべて緑になれば完了です。

---

## 足りないものの一覧

| # | 足りないもの | 誰が | 所要 | これが無いとどうなるか |
|---|---|---|---|---|
| — | ~~公開URL（Cloudflare へ公開）~~ | — | — | **✅ 完了済み**（`https://sakura-japan-beauty.kojanto-jp.workers.dev`） |
| — | ~~Stripe アカウントの本人確認~~ | — | — | **⏳ 提出済み・審査中**（待つ間もテスト環境で進められます） |
| 3 | **USD / TWD / KRW の確定価格** | SAKURA | 判断 | 金額を決められない |
| 4 | **Stripe の商品と価格（Price ID）6つ** | SAKURA | 15分 | 決済ページを作れない |
| 5 | `STRIPE_SECRET_KEY` | SAKURA | 3分 | Stripe に接続できない |
| 6 | `STRIPE_WEBHOOK_SECRET` | SAKURA | 5分 | **支払っても受講権限が付かない** |
| 7 | `NEXT_PUBLIC_SITE_URL` | SAKURA | 1分 | 決済後の戻り先が正しくならない |
| 8 | **Price ID をコードへ反映** | Claude | 5分 | 購入ボタンが押せないまま |
| 9 | **価格を「確定」に切り替え** | Claude | 1分 | 価格が「準備中」のまま |
| 10 | メール送信（Resend） | SAKURA | 30分 | 購入完了メールが届かない（決済自体は動く） |
| 11 | 購入時の同意文言の確定 | 専門家 | — | 海外の解約権に対応できない |

---

## 1〜2. 完了済み

- **公開URL：** `https://sakura-japan-beauty.kojanto-jp.workers.dev`
- **Stripe の本人確認：** 提出済み・審査中

**審査を待つ間も、テスト環境で 3〜9 を進められます。** 先に進めてください。

---

## 3. 各市場の確定価格を決める

**初回販売の3市場ぶんだけ決めれば足ります。**

**為替の自動換算はしません。** 市場ごとに「その国でいくらで売りたいか」を決めます。
日本円の暫定案（通常 59,800 / ローンチ 39,800）は、金額を考えるときの目安としてお使いください。

| 市場 | 通貨 | 通常価格 | ローンチ価格 |
|---|---|---|---|
| 海外（米ドル） | USD | ？ | ？ |
| 台湾 | TWD | ？ | ？ |
| 韓国 | KRW | ？ | ？ |
| ~~日本~~ | ~~JPY~~ | （P1。今回は不要） | （P1。今回は不要） |

決まったら Claude に伝えてください（8・9 で反映します）。

> **韓国ウォンの注意：** Stripe では KRW に小数がありません。
> 「39800」のように、そのままの金額を入れてください。

---

## 4. Stripe で商品と価格を作る

1. Stripe の画面右上の **「テスト環境」をオン**にする（まずテストから）
2. 左メニュー **「商品カタログ」** →「商品を追加」
3. 名前：`Japanese Salon Standard`
4. 価格を入力して保存
5. 同じ商品の画面で **「別の価格を追加」** を押し、通貨を変えて繰り返す

**作る価格は全部で6つです。**（日本円は P1 なので今回は作りません）

| | 通常価格 | ローンチ価格 |
|---|---|---|
| USD | 1つ | 1つ |
| TWD | 1つ | 1つ |
| KRW | 1つ | 1つ |

> **なぜ2つずつ必要か：** Stripe の価格は**金額を後から変えられません**。
> 通常価格とローンチ価格は別の価格として作る必要があります。

6. 作った6つの **`price_` で始まるID** をすべてコピーしてメモする

> どのIDがどの市場・どちらの価格かが分かるように控えてください。例：
> `USD 通常 = price_xxx` / `USD ローンチ = price_yyy` / `TWD 通常 = …`

→ このIDは秘密情報ではありません。**そのまま Claude に伝えてください。**

---

## 5. `STRIPE_SECRET_KEY` を取得する

1. 左メニュー **「開発者」** → **「APIキー」**
2. **「シークレットキー」** の「表示」を押してコピー
   - テスト環境：`sk_test_` で始まる
   - 本番環境：`sk_live_` で始まる

> ⚠️ `pk_` で始まるほう（公開可能キー）ではありません。**`sk_` のほう**です。
> ⚠️ この値は**チャットに貼らないでください**。設定場所は `docs/env-setup.md` を見てください。

---

## 6. `STRIPE_WEBHOOK_SECRET` を取得する

**これがいちばん大事です。** これが無いと、お客様が支払っても受講できるようになりません。

1. 左メニュー **「開発者」** → **「Webhook」**
2. **「エンドポイントを追加」** をクリック
3. 「エンドポイントURL」に次を入力

   ```
   https://sakura-japan-beauty.kojanto-jp.workers.dev/api/stripe/webhook
   ```

4. 「リッスンするイベント」で **`checkout.session.completed`** を選ぶ（**これ1つだけで足ります**）
5. 保存すると詳細画面に **「署名シークレット」** が出ます。「表示」を押してコピー
   - `whsec_` で始まります

> テスト環境と本番環境で**別々の値**です。切り替えたら必ず取り直してください。

---

## 7. `NEXT_PUBLIC_SITE_URL` を設定する

次の値を設定します（末尾の `/` は付けません）。

```
https://sakura-japan-beauty.kojanto-jp.workers.dev
```

決済後にお客様が戻ってくる先と、メールに載せる受講画面のURLが、この値から作られます。
**設定を忘れると `localhost` へ戻そうとして、購入者が戻ってこられません。**

---

## 8〜9. Claude 側の作業

3 の確定価格と 4 の Price ID をもらったら、Claude が次を行います。

- `src/lib/data/mock/catalog.ts` の `pricing.byMarket` に、3市場の金額と Price ID を入れる
- `pricing.status` を `'draft'` から `'confirmed'` に変える
- 全検査を流して push する

→ ここまでで**購入ボタンが押せるようになります。**

> 日本円（P1）を追加するときも同じ手順です。JPY の価格を Stripe で2つ作り、
> その Price ID を渡していただければ、`byMarket` に `jp` を足します。

---

## 10. メール送信（Resend）

購入完了メールを送るために必要です。**決済そのものは 9 までで動きます。**
手順は `docs/env-setup.md` の「③ Resend」を見てください。

---

## 11. 購入時の同意文言（専門家の確認）

EU・英国などでは、デジタルコンテンツに法定の解約権があります。
「すぐ視聴できる状態にすることに同意し、解約権が消えることを承知した」という
明示的な同意を取らないと、「原則返金不可」が通らない可能性があります。

**チェックボックスと記録の実装は終わっています。文言だけが未確定です。**
文面は `src/lib/consent.ts` にあります。専門家の確認後に差し替えてください。

---

## テストから本番へ切り替えるとき

本番の本人確認が通ったら、次の3つを**本番用の値に差し替えます**。

1. `STRIPE_SECRET_KEY` → `sk_live_` で始まるもの
2. 本番環境で Webhook を登録し直し、`STRIPE_WEBHOOK_SECRET` → 新しい `whsec_`
3. 本番環境で商品と価格を作り直し（6つ）、**新しい Price ID を Claude へ伝える**

> テスト環境で作った Price ID は本番では使えません。必ず作り直しが要ります。

---

## 通しテストのやり方（テスト環境）

1. テスト用のカード番号 `4242 4242 4242 4242`（有効期限は未来、CVC は任意の3桁）で購入する
2. 確認すること
   - 決済後に「ご購入ありがとうございます」が出る
   - **マイページの受講中講座に出てくる**（＝ Webhook が動いた証拠）
   - 購入完了メールが**購入時の言語で**届く
   - Stripe の「支払い」画面に記録が残っている
3. うまくいかないときは、Stripe の **「開発者」→「Webhook」→ 該当エンドポイント** を開き、
   送信結果が成功（2xx）になっているかを確認してください
